# Status

- ADR-0081 adds a fail-closed self-hosted Supabase compatibility boundary without changing production. The exact `self-hosted/v0.8.0` source/tag/commit, two reviewed Compose variants, fifteen mounted assets, eleven `linux/amd64` image IDs, Envoy/Auth/PostgREST/PostgreSQL/postgres-meta/JWKS behavior, and all 173 tenant tables are now machine checked through 39 deterministic corruptions. The audit found and additively enabled RLS on three private coordination tables that already had direct grants revoked. Read-only VM 971 evidence matches the reviewed source files and healthy service inventory; its older Studio container still lacks `loyalty` schema parity and requires an approved isolated recreation/smoke test. Exact future upgrade rehearsal and clean-room restore also remain open, and no production container, file, database, checkout path, or loyalty value was changed.
- ADR-0080 now separates the deployed-production and integration-candidate whole-product scores. Production `v0.1.11` is the only completion subject and remains 54/100; the exact unmerged candidate is the development-prioritization subject at 83/100 because M04-M14 implementation breadth is now represented. Both remain completion-ineligible: activation is 3/10, required live evidence is absent, and no score can replace the real-store pilot, module canaries, recovery, security, observation, reconciliation, or approvals. The fixed-digest V1 history and the V2 score validator prevent history rewriting, arithmetic drift, and candidate-as-production claims.
- Merge-readiness review repaired three concrete boundaries without changing production: ADR-0079 limits the public reader to complete V6 then released English V1, V6 now carries exact immutable programme currency for non-EUR earning/VIP formatting, and expanded reward rows retain editor-only identity through edits/removal. M09 now uses the shared manifest envelope and all eleven M04-M14 validators share one no-follow digest-bound JSON artifact reader. The complete local gate passes 983 tests, every validator/typecheck/build, 87 migrations and 69 pgTAP files, a 1,076-file secret scan, zero production dependency vulnerabilities, and licence validation; exact-head GitHub CI/database replay remains the handoff authority.
- ADR-0071's undeployed Borg candidate now has one installed fail-closed `archive|maintain` controller with a single configuration boundary and shared numeric-state/repository-metric primitives. Both systemd units select explicit modes; the old standalone programs remain rollback artifacts. No production script, timer, repository, credential, archive, or recovery state changed.

## Enterprise roadmap status

- Production application images run `v0.1.11`; M00, M02, and M03 are complete, and M01 plus M04–M16 remain the authoritative unfinished enterprise graph.
- M09's hosted guest VIP follow-up is repository-verified under ADR-0074. An additive strict V3 projection now represents advanced lifetime/rolling/calendar qualification, `all`/`any` entry thresholds, exact bigint-safe metrics, grace, earning rates, and safe benefit flags instead of mislabelling every tier as legacy spend. Its original V2/V1 rollout bridge remains historical evidence; ADR-0079 supersedes the application reader with complete V6 then released English V1 only. Malformed current data still fails closed. The responsive progression rail passed focused desktop/mobile Playwright review with zero diagnostics, and exact implementation head `7a68ffa` passed all 12 PR checks in CI `33157341807` and Security `33157341670`, including 82-migration replay and 3,712 pgTAP assertions. Production and M09's 88/100 canary state remain unchanged.
- M09's next dependency-safe hosted correction is repository-verified under ADR-0075. Strict additive V4 projects at most twelve published standard earning methods with derived public codes/labels, exact effects, safe schedules, and a conservative restrictions signal while excluding merchant-authored identifiers/copy, custom activities, selectors, caps, customer state, internal IDs, and value authority. Its original V3/V2/V1 rollout bridge remains historical evidence; ADR-0079 supersedes the application reader with complete V6 then released English V1 only. Malformed current data still fails closed. The new catalogue passed focused desktop/mobile reduced-motion browser review across all five public sources with zero overflow or diagnostics. Exact implementation head `d91a2d7` passed all 11 required checks in CI `33161466635` and Security `33161466605`, including 83 migrations, 68 pgTAP files, and 3,725 assertions. Production is unchanged, and M09 remains honestly 88/100 until its approved canary.
- M09's guest reward discovery correction is repository-verified and undeployed under ADR-0076. Strict additive V5 removes legacy raw reward rows from the browser contract and projects supported published benefits, exact point/money evidence, schedules, summarized public conditions, and native/manual delivery while excluding stored value, raw selectors, instructions, exact limits/budgets, internal codes/IDs, segment/customer state, audit evidence, and value authority. Its original V4/V3/V2/V1 rollout bridge remains historical evidence; ADR-0079 supersedes the application reader with complete V6 then released English V1 only. Malformed current data still fails closed. The new catalogue passed focused reduced-motion desktop/mobile review across all six supported benefit presentations with zero overflow or browser diagnostics. Exact implementation head `294c62a` passed all 11 required checks in CI `33165531738` and Security `33165531707`, including 84 migrations, 68 pgTAP files, and 3,740 assertions. Production is unchanged, and M09 remains honestly 88/100 until its approved canary.
- M09's guest referral discovery correction is repository-verified and undeployed under ADR-0077. Strict additive V6 publishes only the immutable programme-level give-and-get points, minimum first-order spend/currency, attribution and cooling windows, new-customer rule, monthly-limit signal, and database-authoritative available/paused/unavailable state. Customer links, identities, orders, history, risk evidence, exact abuse caps, internal IDs, raw configuration, and value authority remain private. ADR-0079 supersedes the original intermediate fallback chain: the application now requests complete V6 then released English V1 only, and malformed V6 fails closed. Production-rendered reduced-motion review passed at 1512×982 and 390×844 with exact terms, a same-origin private-account action, 44-pixel targets, zero overflow, and zero browser diagnostics. Exact implementation head `3812e67` passed all 11 required checks in CI `33169816691` and Security `33169816719`, including 85 migrations, 68 pgTAP files with 3,753 assertions, all 22 concurrency probes, both images, and all four WooCommerce runtime cells. Production is unchanged, and M09 remains honestly 88/100 until its approved canary.
- M09's authenticated campaign-discovery correction is repository-verified and undeployed under ADR-0078. Strict additive customer V3 accepts no selectors and derives the Auth subject, active account link, programme, wallet, immutable treatment assignment, lifecycle, and remaining capacity in PostgreSQL. It exposes only bounded safe scheduled/active purchase bonuses and multipliers with exact bigint/basis-point and combination evidence; control, cross-tenant, revoked, paused, ended, exhausted, raw selector, budget, liability, identity, and value facts remain absent. V2/V1 are missing-function-only fallbacks and malformed V3 fails closed. Production-build review passed at 1512×982 and 390×844 with exact extreme values, one H1, unique IDs, zero overflow, and zero diagnostics. Exact implementation head `9644d66` passed CI `33175790670` and Security `33175790673`: 985 workspace tests, 86 migrations, all 68 pgTAP files with 3,772 assertions, all 22 concurrency probes, both images, and all four WooCommerce runtimes passed. Production is unchanged, and M09 remains honestly 88/100 until its approved canary.
- The exact integration candidate's engineering quality is 95/100. Whole-product readiness is separately 54/100 for deployed production and 83/100 for the unmerged candidate; implemented M04-M14 breadth is no longer described as absent, while every live activation and closeout gate remains open.
- The old local Phase 4 branch and six modified planning files are preserved in a named stash. M00, the M01 evidence gate, M02, and M03 repository implementation merged through PRs #22–#26; active work proceeds from integrated `main`.
- M04–M14 now use one registered exact canary-envelope catalogue. The complete family fails closed on unreviewed root or nested fields, unknown schemas, future timestamps, evidence strings above 4 KiB, cycles, and invalid task graphs; 154 deterministic corruptions cover fourteen boundary failures for each of eleven modules. Every focused module validator passes while preserving its prior score and pending production controls. This is evidence-integrity hardening only, not deployment or canary completion.
- M01 is active for an approved real WooCommerce store and full-service recovery proof. A fresh read-only baseline now records healthy production v0.1.11, zero commerce/value rows, successful current PostgreSQL backup jobs, and one successful nightly Borg inventory containing both VM 970 and 971. The exact validator rejects contradictory store/restore authority, unreconciled counts, mutable release identity, sensitive values, and false completion through eleven adversarial fixtures. Archive creation is not a restore: the controlled real-store sequence, full application/Auth/Authentik/configuration/signing recovery, alerts, reconciliation, and approvals remain pending, so the manifest honestly remains one of 22 checks passed. Its external gate does not block dependency-safe repository work.
- Read-only VM 971 follow-up confirms the historical 3.604 TB counter is quiet—about 190 MiB over the latest 24 hours—but found the incremental PostgreSQL timer silently returning success while the nightly whole-VM Borg job held the shared repository lock. Live timing disproved bounded waiting as a complete fix: one VM archive held the repository for 22 minutes 20 seconds, the multi-guest job held the external lock for 1 hour 45 minutes 13 seconds, the first post-release retry still hit Borg's remote lock, and the off-host PostgreSQL archive gap reached 1 hour 50 minutes 39 seconds. ADR-0071's undeployed candidate requires a distinct actual PostgreSQL Borg repository/lock/cache, stages before locking, preserves every recent archive for 48 hours before daily/monthly thinning, and fails status 75 on dedicated contention. ADR-0072 adds a second fail-closed gate after read-only package inspection found the Debian host on rsync 3.4.1 and Ubuntu guest on 3.2.7 despite directly applicable upstream 3.5 security fixes: both endpoints must use canonical trusted 3.5-or-newer executables, and the forced guest wrapper must prove confinement under a cleared environment. Automated maintenance first requires a fresh archive, bounds every operation, measures the post-prune canonical 48-hour timeline, and refuses compaction when an interval exceeds 300 seconds. Atomic minimized node_exporter metrics now distinguish archive/maintenance success and age, actual repository isolation, retained count, and maximum interval; absent, stale, zero, failed, and over-bound states have immediate protected alerts. Production is unchanged; provenance-checked dual-endpoint upgrade, dedicated-repository provisioning, monitoring activation, timer/retention evidence, and isolated restore remain required.
- A direct 10:27 CEST revalidation observed the next scheduled VM 971 backup cycle end to end: four changed files, 67,885 bytes of file content, 409,323 rsync bytes received, a 1.63-second Borg archive, and about 446 KiB total tap ingress over the watch window. The active timer remained enabled, `/api/healthz` returned 200, and the old PostgreSQL `--content-from-command` path remained only a dated rollback file. The cumulative 3.604 TB Proxmox counter was intentionally not reset by restarting the production VM.
- M02 is complete at 93/100 after exact-head migration replay, 1,095 pgTAP assertions, concurrency/property probes, containers, four WooCommerce runtime cells, a fresh production physical backup, additive v27 migration, managed-mode transition, and the single Starfiniti `programme.v2` canary passed.
- M02 defaults upgrades to locally controlled `self_hosted`, keeps managed billing off, and makes PostgreSQL—not Auth claims, the browser, Stripe, or a licence server—the entitlement authority. Six protected value paths cannot be commercially disabled.
- M03 is complete at 93/100: the strict V2 contract, shared exact evaluator, WooCommerce and signed activity inputs, database-authoritative publication/cap/ledger boundaries, and Hub-style rule builder/simulator passed PR #26 exact-head run `31736396618`, release run `31738294379`, additive production migration, explicit Starfiniti tenant canary, and authenticated desktop/mobile production smoke.
- M04-S03 is complete. The real reward editor and manual-fulfilment queue passed optimized-build native-Chrome review at 1440, 390, and 320 pixel widths with named controls, keyboard/drawer focus, reduced motion, zero overflow, zero diagnostics, 12 px minimum control text, and 44 px M04 mobile targets. The review repaired an invalid-draft failure that reclassified V2 rewards as legacy, added exact field-level validation/focus, and kept Save available for submit-time guidance. The full repository gate passed with 958 tests, 81 migration/68 pgTAP-file validation, secret/licence checks, and zero production dependency vulnerabilities. M04-S04 now has a 48-check fail-closed canary validator and nine named path-, SHA-256-, and semantic-bound production artifacts whose schemas reconcile approved policies/ceilings, exact release/plugin/contracts, native/manual/recovery evidence and counts, zero-difference value/queue/privacy outcomes, canonical chronology, rollback after canary end, and at least 24 hours of observation. Thirteen repository/read-only controls pass, 35 approval/deployment/canary/reconciliation controls remain pending, and the provisional 90/100 score cannot close while operability is 3/10. Approved release, pilot store, recovery point, disabled deployment, Starfiniti canary, reconciliation, observation/rollback, and final scoring remain open; production was unchanged.
- M13 deployment hardening under ADR-0069 separates live workforce SSO from disabled per-organization federation administration. A fresh public authorize probe still redirects `custom:starfiniti-sso` to Authentik, while read-only VM 970 inspection confirms production v0.1.11 has none of the three tenant-federation administration files. Candidate preflight accepts an empty set, rejects a partial set, and strictly validates a complete set; the exact candidate Compose with read-only `/dev/null` binds validates on the production host's Docker Compose 2.40.3 without starting or changing a container. Production, Auth, tenants, credentials, checkout, and loyalty value remain unchanged.
- M13-S01 through S05 are repository-complete on stacked draft PRs #40 through #44 with all production capability disabled. The latest inherited exact-head run `33013504755` at `8587841` passed root checks, both images, a clean 75-migration replay, all 62 pgTAP files with 3,349 assertions, all 16 concurrency probes, and all four WooCommerce runtimes. Tenant OIDC/SAML, Authentik hashed-subject SCIM, invitation-or-SCIM authority, immediate deprovisioning, bilateral agency, short approved support, signed-AAL2 owner recovery, bounded export, credential offboarding, and cooled pseudonymization are implemented without making claims, email, domain, groups, browser selectors, or agency relationships authoritative. The final adversarial pass also repaired terminal webhook destination/fingerprint survival.
- M13-S01 through S05 are repository-complete under ADR-0051 through ADR-0055 and production capabilities remain disabled. Integrated candidate `0ae43ea` passed CI `33104114747` and Security `33104114894`. M13-S06 now has a 51-check, four-approval, nine-artifact fail-closed gate with safe bounded reads, exact semantic detail schemas, unique zero-difference source assertions, cross-artifact approval binding, canonical chronology, a minimum 24-hour canary-covering observation, sensitive-key/value rejection, fixed automatic failures, non-canary isolation, prerequisite/task synchronization, and corruption fixtures. Fresh read-only Loyalty, Supabase, Authentik, DNS, Proxmox, and VM evidence passes without mutation. Twelve controls pass and 39 approved production controls remain pending; the provisional score is 90/100, but operability is 3/10 and blocks close. Production is unchanged.
- M14-S01 is repository-complete on stacked draft PR #46 under ADR-0056. It adds a strict minimized billing contract, private append-only account/state evidence, live-membership projection, stable event-time ordering, caller-key-independent provider-customer and provider-event replay fencing, a structural return before provider creation in `self_hosted`, and a real Hub-style Billing & plan route. Exact-head run `33020484560` at `68479f1` passed all seven jobs, and real-component desktop/mobile light/dark browser review passed. No provider action or production mutation was introduced.
- M14-S02 is repository-complete on the same draft branch under ADR-0057. The public route gates through PostgreSQL before reading body or secret, verifies bounded exact raw bytes with HMAC/timestamp/rotation handling, persists only a strict immutable digest-bound projection, and queues an isolated no-provider worker that rechecks entitlement before subscription normalization while treating invoices as observations. Exact-head run `33024281886` at `5b7ff26` passed all seven jobs: root checks, both images, a clean 77-migration replay, all 64 pgTAP files with 3,477 assertions including all 67 focused webhook cases, all 18 concurrency probes, and all four WooCommerce runtimes. Production, global `self_hosted` mode, Stripe configuration, the optional `billing` profile, and all loyalty value remain unchanged.
- M14-S03 is repository-complete on stacked draft PR #47 under ADR-0058. It adds strict public plan/session contracts, private effective Price and operation evidence, per-tenant customer-provisioning serialization, database reauthorization before every provider request, a regular-file restricted-key boundary, fixed-origin/versioned/bounded Stripe REST calls, validated official redirects, webhook-only lifecycle authority, and responsive owner-only plan/Portal controls. Exact-head run `33030547427` at `d72166a` passed root checks, both images, a clean 78-migration replay, all 65 pgTAP files with 3,532 assertions including all 55 focused session cases, all 19 concurrency probes, and all four WooCommerce runtimes. Production-rendered desktop/mobile owner/read-only/unavailable review passed. Production and global `self_hosted` remain unchanged; S04 metering is repository-complete and S05 commercial-state policy is active, while sandbox credentials, approved Price IDs/meters, test clocks, and canary approval remain deferred.
- M14-S04 is repository-complete on stacked draft PR #48 under ADR-0059. Four exact UTC source-fact meters, append-only source-bound compensation, permanent provider identifiers, database-derived leases and effect-time reauthorization, a separate regular-file restricted-key Stripe sink, and a live-member usage-health projection add bigint-safe managed billing without creating loyalty-value or checkout authority. The final adversarial review repaired pre-activation backbilling, source-period drift and standalone-negative correction routing, missing five-minute policy-hold cooling, public configured-state mismatches, one dormant capture ambiguity, and the concurrent fact/identifier race. Implementation-head run `33036216639` at `c7f72ec` passed all seven jobs: 79 migrations, all 66 pgTAP files with 3,595 assertions including 63 focused usage cases, all 20 concurrency probes, 939 workspace tests, both images, and all four WooCommerce runtimes. Desktop/mobile light/dark production-rendered review also passed. Production/global `self_hosted` are unchanged; S05 deterministic delinquency/manual-contract policy is active, while sandbox meters, restricted key, invoice reconciliation, and canary approval remain deferred.
- M14-S05A is repository-complete on stacked draft PR #49 under ADR-0060. Append-only delinquency policy and separately approved manual contracts resolve deterministically against immutable provider occurrence evidence; explicit grace wins, later-observed backdated policy cannot rewrite old outcomes, semantic same-instant retries converge, conflicts fail independent of lock order, and V1 remains compatible while minimized V2 adds source/reason/contract term. Exact-head run `33038559023` at `877f7e9` passed all seven jobs: 945 workspace tests, both images, a clean 80-migration replay, all 67 pgTAP files with 3,658 assertions including 63 focused commercial-policy cases, all 21 concurrency probes, and all four WooCommerce runtimes. Production-rendered restricted/grace/contract desktop/mobile light/dark review passed. Production/global `self_hosted` are unchanged; S05B and S05C are now repository-complete.
- M14-S05B is repository-complete on stacked draft PR #49 under ADR-0061. A private immutable 23-root inventory installs ordered commercial guards only on reviewed mutable growth/configuration tables, checks only tenant-canary `managed.billing`, preserves established product validation and mixed V1/V2 history, permits safe risk-reducing lifecycle changes, and structurally excludes ledger/value, commerce, customer-access, export, organization-access, correction, and checkout roots. Database-role-first bypass prevents stale request claims from commercializing operator recovery while private actor validation preserves subjectless runtime commands. Exact-head run `33041473615` at `100c164` passed all seven jobs: 945 workspace tests, both images, a clean 81-migration replay, all 68 pgTAP files with 3,701 assertions, all 22 concurrency probes, and all four WooCommerce runtimes. Production/global `self_hosted` and all managed-billing canaries remain unchanged; S05C is repository-complete and S06 is active.
- M14-S05C is repository-complete on stacked draft PR #49. The Billing route now provides authority-aware recovery and operational-continuity paths in every commercial state, hides self-serve provider controls for manual contracts, fails zero-plan configuration honestly, preserves protected value/checkout/export guidance, and renders the existing Starfiniti icon. Adversarial browser review repaired contradictory controls/copy and a focused skip-link collision; the final four-viewport light/dark reduced-motion matrix passed landmarks, keyboard/drawer focus, reflow, exact bigint display, diagnostics, and measured WCAG contrast. Exact implementation-head run `33043411759` at `f7a4def` passed all seven jobs: 950 workspace tests, both images, a clean 81-migration replay, all 68 pgTAP files with 3,701 assertions, all 22 concurrency probes, and all four WooCommerce runtimes. M14-S05 is repository-complete; S06 fail-closed canary and closeout is active while production/global `self_hosted` remain unchanged.
- M14-S06 now has a 49-check, five-approval, nine-artifact fail-closed canary manifest wired into `npm run check`. Thirteen repository/read-only controls pass and 36 approved release, sandbox/catalogue/policy, recovery, deployment/isolation, lifecycle, usage, invoice, outage, rollback, observation, and reconciliation controls remain pending. Adversarial reproduction proved digest binding alone accepted an empty artifact body; exact per-artifact semantics, check-level source digests, zero-difference assertions, cross-artifact final approval, canonical chronology, and a 24-hour observation floor now close that bypass. Integrated candidate `9bea3d4` passed CI `33105154521` and Security `33105155321` with all eleven PR checks green: 958 workspace tests, both images, a clean 81-migration/68-pgTAP-file validation, the complete concurrency matrix, and all four WooCommerce runtimes. The provisional score is 90/100, but operability is 3/10 and blocks completion. Fresh read-only evidence confirms canonical public/Auth/REST behavior and running VMs 970/971; production remains globally `self_hosted` with no Stripe configuration, key mount, worker, provider request, managed canary, or loyalty-value mutation.
- M15-S01 is active under ADR-0062. A repository-owned fixed-arrival driver concurrently exercises readiness, authenticated customer account reads, scoped Service API customer intake, and exact-body signed WooCommerce order intake with unique phase-safe identities. It binds a clean commit plus origin/workload digests to a short approval, reads authority only from separate owner files, refuses known production mutation, caps samples/concurrency/timeouts/responses and two million total requests, distinguishes driver saturation, validates response contracts, and emits minimized aggregate evidence. Exact implementation-head run `33046822172` at `10b0728` passed all seven jobs. The 22-check gate records six repository checks passing and 16 environment/inventory/data/monitoring/load/latency/reconciliation/repeatability/independent-driver/claim checks pending. No supported capacity is claimed and no production load or mutation has run.
- M15-S02 is active under ADR-0063. Its six exact scenarios cover worker death, database crash/restart, database-path latency, byte-identical duplicate delivery, provider outage, and a bounded outage retry burst. The runner requires a clean exact commit, short approval, separate marked sandbox, exact file/fixture digests, prefixed Compose project, disposable non-production container labels, and loopback-only public/proxy origins; it restores in `finally`, stops after any failed scenario, probes readiness plus native checkout, and emits only aggregates. Exact implementation-head run `33049635069` at `adf6d9d` passed all seven jobs. The 27-check manifest records six repository controls passing and 21 environment/routing/monitoring/fault/WAL/queue/value/reconciliation checks pending. No production fault, disposable exercise, raw fixture, or customer data has been used.
- M15-S03 is active under ADR-0064. Exact implementation-head CI run `33057428413` and Security run `33057428402` at `2a27fc6` are fully green: all seven baseline/database/container/WooCommerce jobs, CodeQL, repository scan, both strict deployable-image scans, the exact worker runtime import probe, both CycloneDX inventories, and isolated ZAP passed. The repaired worker image carries only its three external runtime packages instead of the monorepo root tree; R-032 is resolved by a zero-vulnerability complete audit plus all four Linux WooCommerce cells. The 25-check manifest now records 15 checks passing and ten release/freshness/triage/production/independent-test/approval gates non-passing. No production DAST, credential, customer data, provider call, or loyalty-value mutation was authorized.
- M15-S04 is active under ADR-0065, ADR-0071, ADR-0072, and ADR-0073. The repository has a fourteen-stage full-service clean-room controller plus an exact two-endpoint rsync 3.5 candidate: digest-pinned Debian 13 and Ubuntu 24.04 images acquire exact Debian archive and rsync-project Launchpad packages through scoped signed metadata, verify package checksums/metadata, canonical executables, protocol 32, upstream wrapper confinement, and a negative restricted command, then transfer only two synthetic files over a disposable internal Docker network with bounded minimized evidence and exact teardown. Earlier candidate `13e55ad` remains historical candidate-package evidence. Rollback-aware implementation `ed5eb7f` passed Security run `33151832310`, job `98785369076`; artifact `9678028203` binds the exact pre-change Debian Security host rsync, Debian host `libacl1`, and Ubuntu Security guest rsync proofs, signed-metadata and exact-URL byte equality, checksums/metadata, zero installation and retained package bytes, protocol 32, bounded transfer, no production mutation, and teardown. Operations escrow, host-consumer compatibility, approved dual-endpoint installation, real forced-command/manual/timer archives, dedicated repository, two full-service recoveries, and independent reconciliation remain required. Read-only revalidation found both VMs running and the host still on the bound rsync/libacl versions; the guest agent and host-to-guest SSH were unavailable, no dedicated PostgreSQL backup configuration or active monitoring plane was found, and no secret content was read. No production package, script, service, timer, repository, backup, route, checkout path, or loyalty value changed.
- M15-S05 is active under ADR-0066 on draft PR #54. Read-only production inspection found no active Prometheus, Alertmanager, Grafana, Loki, Promtail, or node-exporter unit and confirmed the approximately 3.60 TB VM 971 transmit value is cumulative history while the latest twelve one-minute egress samples peak below 7 KB/s. The repository adds a closed 24-signal/23-alert bounded-label catalogue with an absent-or-incomplete required-series page, exact Prometheus rules, a billing-independent route policy and external dead-man requirement, locked Grafana provisioning, seventeen alert-linked runbooks including dual-threshold backup amplification, monotonic SEV0–SEV2 incident states, a postmortem contract, and a 34-check digest-bound completion validator. Exact final head `eaf50ec` passed all seven CI jobs in run `33066217163`, all three Security jobs in run `33066217160`, and the external CodeQL policy check; PR #54 is clean. Thirteen repository/reality/CI checks pass and 21 approved-environment, source, route, dashboard, owner, exercise, reconciliation, and approval checks remain pending. Production monitoring, receivers, credentials, configuration, checkout, and loyalty value are unchanged.
- The integrated M15-S05 candidate extends that gate to 31 bounded signals, 27 exact alerts, 13 dashboard panels, and 35 checks. Linux fixtures bind actual dedicated-repository identity, atomic textfile publication, archive attempt/freshness, post-prune retained interval, failure before compaction, and the non-authoritative telemetry boundary. The ADR-0071 controller now opens and validates its owner-only configuration once through a descriptor, rejects unsafe directory ancestry before external action, emits exact per-cycle transfer/amplification gauges, and invokes no Borg command after malformed or dual-threshold transfer evidence. Exact hardening head `ccaa928` passed all seven CI jobs in run `33140104569`, all three Security jobs in run `33140104565`, and external CodeQL; 14 controls pass and the same 21 production activation, routing, exercise, reconciliation, and approval controls remain open. Production has no monitoring plane and the new recovery controller, metrics, and rules are not deployed.
- M15-S06 is active under ADR-0067 at implementation candidate `900ffbc`. A canonical plan now fixes one pilot tenant, at least 720 consecutive UTC hours and thirty complete daily intervals on one immutable release, five distinct minimized artifacts, current capacity/fault/security/recovery/operations digests, eleven daily and fifteen final zero-difference fields, exact module and score floors, six approval roles, and restart-on-drift rules. Thirteen claims default to non-publishable; Shopify, languages beyond English, and cash-like stored value are explicit limitations. The 50-check validator proves a valid synthetic closeout and rejects false completion, short/discontinuous windows, drift, nonzero differences, unsafe/reused evidence, incomplete modules, score failures, unapproved claims, and missing approvals. Documentation head `6b57148` passed all seven CI jobs in run `33068012282`, all three Security jobs in run `33068012381`, and external CodeQL policy. Five repository controls pass; 45 release, deployment, live canary, reconciliation, security, recovery, operations, claims, M16 handoff, and approval checks remain pending. The honest provisional score is 77/100 with performance and operability below their floors. Production remains unchanged.
- M16 repository implementation is active under ADR-0068. Its canonical UTC contract requires two distinct consecutive monthly reviews, one five-exercise quarterly bundle, fourteen review sections, thirteen official provider/platform/recovery sources, exact backlog ranking, second-occurrence durable controls, material-change rescoring, guarded experiments, append-only ADR supersession, five digest-bound closeout artifacts, independent review, and five approvals. The recovery set now includes rsync, BorgBackup, OpenSSH, Debian, Ubuntu, and Proxmox's current Security Advisories category; the 39-check validator rejects missing, stale, or substituted sources, missing catalogue-declared host/guest endpoints, and absent/zero installed or candidate provenance digests. The live pre-3.5 rsync boundary is recorded as the top exact backlog item at 80 rather than being hidden by its external rollout dependency. Implementation head `a7a00f2` passed CI `33144201269`, Security `33144201312`, and external CodeQL with all eleven PR #57 checks green. Seven repository controls pass and 32 elapsed-cadence, source, exercise, reconciliation, review, and approval checks remain pending; the provisional score is 77/100 with performance and operability below their floors. No elapsed review, schedule, owner identity, package upgrade, experiment, exercise, deployment, checkout path, or loyalty value changed.
- Delivery-chain review repaired the only non-external merge and database blockers. PRs #29/#30/#31/#32 are exact-head green at `30fb2f9`/`48c8784`/`f16d6b2`/`847f16e` in runs `33073236099`/`33074423308`/`33075109579`/`33075499745`; PR #42 is exact-head green at `56e854a` in run `33073430984`. Every open roadmap PR from #29 through #56 now reports `CLEAN` with zero failed or pending checks. These results prove merge readiness, not deployment, canary, reconciliation, or module completion.
- Consolidated integration review found and repaired three material fail-closed gaps: legacy PKCE fallback without exact flow correlation or a hostname-independent shared verifier namespace, Checkout/Portal redirect exposure after a final PostgreSQL hold, and unrestricted Stripe key acceptance in the metering-only worker. It also unified executable migration limits, bounded older operational artifact readers, removed three whitespace failures, and corrected the recovery-gate count. External CodeQL then exposed five check-then-read races and one polynomial padding expression, all corrected; alerts #15 and #16 were auditably dismissed as false positives for a read-only existing fault input and a high-entropy Klaviyo credential fingerprint. Earlier security-remediated candidate `82f644c` on PR #57 passed local root/database/secret/audit/licence gates, all seven CI jobs in run `33084847238`, all three Security jobs in run `33084847239`, and external CodeQL check `98562118454`; it was clean and mergeable with zero open Critical/High alerts. PR #57 is now ready, and its current-head rollup remains the handoff authority. Production and every live gate remain unchanged.
- M10-S01 through S05 are complete. Dictionary V4's 103 definitions and four independently degradable reports feed a strict aggregate JSON export with private 24-hour payloads, one-use downloads, owner/admin schedules, and an isolated reporting worker. The command center binds all modules to one exact snapshot and passed Hub-style desktop/mobile, loading, stale, empty, partial-error, keyboard, reduced-motion, contrast, overflow, and English-only review. Integrated candidate `b760cec` passed CI `33101099338` and Security `33101099291`. M10-S06 now has a 32-check, four-approval, nine-artifact fail-closed gate with safe bounded reads, exact semantic schemas, approved scope/policy binding, shared-snapshot/report/export/schedule reconciliation, explicit liability/causal labels, zero-difference value/privacy assertions, cross-artifact final approval, canonical chronology, minimum 24-hour observation, fixed failure rules, and positive/adversarial fixtures. Eleven controls pass and 21 approved production controls remain pending; the provisional score is 90/100, but operability is 3/10 and blocks close. Monetary liability, mixed-currency conversion, prediction, statistical significance, emailed reports, and production generation remain explicitly unavailable.
- M11-S01 repository and browser gates are complete on `codex/m11-multistore`. ADR-0042 keeps programme groups as the wallet boundary and adds explicit isolated/shared workspace policy, immutable revisions, projection reconciliation, Auth-derived owner/admin entitlement authority, idempotent optimistic commands, connector-removal protection, and a Hub-style review-before-save Operations control. Exact-head run `32905613578` at `3cb609d` passed all seven jobs: baseline, both images, clean 63-migration replay, all 50 pgTAP files including 52 focused sharing assertions, all seven concurrency probes, and all four WooCommerce runtimes. Native Chrome desktop/mobile interaction/accessibility review also passed. Disabled deployment, Starfiniti-only canary, topology/wallet/connector reconciliation, rollback, and observation remain pending under M11-S06.
- M11-S02 repository and browser gates are complete. ADR-0043 requires a separate fresh signed store proof on one live Auth subject, rejects secondary customers with existing wallet state, retains immutable source identity/customer evidence, and protects one stable canonical projection through transaction-scoped trigger capabilities. Auth-derived no-selector reads and confirmed non-canonical unlink/relink power a responsive English connected-stores customer experience. Playwright review passed desktop/mobile readability, confirmation, keyboard focus, reduced motion, degraded state, and zero overflow/diagnostics. Exact-head run `32910582010` at `19c24a4` passed baseline, both images, clean 64-migration replay, all 51 pgTAP files with 2,716 assertions including 53 focused link cases, all eight concurrency probes, and all four WooCommerce runtimes. Disabled deployment and the Starfiniti canary remain with M11-S06.
- M11-S03 repository and browser gates are complete. ADR-0044 binds foreign-currency V2 commerce to immutable occurrence-time provider snapshots, exact rational conversion, PostgreSQL-recomputed atomic amounts, source-currency rule visibility, and original-snapshot refund reuse. The English Operations policy surface exposes no credentials or rates. Exact-head run `32918516110` at `6bf137c` passed baseline, both images, clean 65-migration replay, all 52 pgTAP files with 2,774 assertions including 58 focused currency cases, all nine concurrency probes, and all four WooCommerce runtimes. Production conversion remains disabled pending an approved provider and M11-S06 canary.
- M11-S04 repository and browser gates are complete. ADR-0045 adds digest-only, least-privilege service credentials with one-time issuance, bounded rotation, immediate revocation, PostgreSQL-derived tenant/workspace/programme/connection/scopes, a randomized opaque customer namespace, a serialized fixed-minute quota, and customer/activity routes that preserve the canonical event/effect/ledger pipeline. Exact-head run `32927596360` at `479f605` passed baseline, both images, clean 66-migration replay, all 53 pgTAP files with 2,846 assertions including 72 focused service-API cases, all ten concurrency probes, and all four WooCommerce runtimes after the browser-driven repair. Playwright review found and repaired dark confirmation contrast, undersized actions, and displaced checkbox labels; the corrected component passed 16.48:1 contrast, 40-pixel actions, desktop/mobile, reduced-motion, keyboard, English-only, overflow, and diagnostics checks. Production issuance remains disabled for M11-S06.
- M11-S05 repository and browser gates are complete. ADR-0046 adds one-time endpoint signing-key issuance, digest-only storage, disabled-only rotation with bounded overlap, disable-before-authorization, terminal destination scrubbing, immutable lifecycle revisions, and a minimized Auth-scoped health surface without changing the M08 wire format. Exact-signature wrappers derive the actor and tenant inside PostgreSQL; browser, runtime, and worker roles cannot execute the private mutation primitives. Supported dependency-light TypeScript and PHP 8.1 clients share strict Service API bodies plus exact raw-body Standard Webhooks verification and replay identity. Run `32932756596` at `a495433` passed all seven jobs: baseline, both images, clean 67-migration replay, all 54 pgTAP files with 2,905 assertions including 59 focused lifecycle cases, all eleven concurrency probes, and all four WooCommerce runtimes. The eleventh probe serialized concurrent authenticated retries into one endpoint/revision/audit and zero ledger effects. Production-build desktop/mobile browser review passed active/disabled/retired/degraded/read-only states, dark mode, reduced motion, 40-pixel actions, 3-pixel focus, English-only output, zero overflow, and zero diagnostics. M11-S06 disabled deployment and Starfiniti ecosystem canary is active.
- M11-S06 now has a 44-check, four-approval, nine-artifact fail-closed gate with safe bounded reads, exact semantic schemas, approved pilot/rate/value binding, topology/identity/currency/API/webhook count reconciliation, zero-difference value and authority assertions, cross-artifact final approval, canonical chronology, minimum 24-hour observation, fixed automatic failures, sensitive-key/value rejection, prerequisite/task synchronization, and positive/adversarial completion fixtures. Integrated candidate `f171770` passed CI `33101922228` and Security `33101922223`. Eleven repository, client, public-baseline, and operator checks pass; 33 approved release/provider/deployment/topology/identity/currency/API/webhook/outage/reconciliation/rollback/observation checks remain pending. The provisional score is 90/100, but operability is 3/10 and blocks close. PR #57 is now ready and production remains unchanged on `v0.1.11`.
- M12-S01 through S05 are complete under ADR-0047 through ADR-0050. Exact canonical documents/resolutions are re-presented at the database value boundary, live authority is re-derived, source rows are serialized across competing receipts, and opening balances, exact pending releases, FIFO lots, and compensating corrections remain immutable and attributable. Pure bounded exact-shape adapters feed a four-source registry-backed dispatcher; YITH and changed/unknown formats fail before parsing. The responsive English Hub workflow keeps uploads request-local, requires explicit non-email mapping and exact approval re-presentation, exposes minimized reconciliation, and preserves correction after entitlement disablement. Integrated candidate `16753b2` passed CI `33102977731` and Security `33102977636`. M12-S06 now has a 36-check, four-approval, nine-artifact fail-closed gate with safe bounded reads, exact semantic schemas, bounded record-count binding, customer/balance/lot/expiry/liability zero-difference assertions, cross-artifact final approval, canonical chronology, minimum 24-hour observation, fixed automatic failures, approval/task synchronization, and corruption fixtures. Eleven controls pass and 25 approved source/release/deployment/value/reconciliation/rollback/observation controls remain pending; the provisional score is 90/100, but operability is 3/10 and blocks close. No production migration or value import is enabled.
- M04-S01 through S03 are implemented: strict V2 authoring, independent publication/redemption checks, atomic quantity/budget/customer capacity, capability-negotiated WooCommerce execution, audited manual-perk fulfil/reject accounting, and six-template merchant/customer workflows passed PR #29 exact-head run `31747964152`. That run passed the full repository gate, both production images, a clean 30-migration replay, all 30 pgTAP files with 1,302 assertions including 102 focused reward assertions, both concurrency probes, and all four minimum/current HPOS/legacy WooCommerce runtimes. Release hardening keeps accepted manual cases visible and operable after rollout disablement, compares availability by timestamp instant, and rejects unsupported or disguised legacy rewards independently at the authenticated database command boundary. Browser/accessibility review, disabled deployment, Starfiniti canary, reconciliation, and scoring remain.
- M05-S01 and S02 are exact-head green. `TierPolicyV2`, the pure event-time evaluator, Rose/Bloom/Icon migration, private qualification facts, refund compensation, worker integration, and independently checked automatic membership decisions passed runs `31750684991` and `31751858746`; the latter includes a clean 32-migration replay, all pgTAP files, both images, and four WooCommerce runtimes.
- M05-S03 is exact-head green. Tier multipliers, fulfilment-bound tier reward access, owner/admin-only expiring manual overrides, continued underlying automatic qualification, and bounded exactly-once override expiry passed run `31753641793`: clean 33-migration replay, all 31 pgTAP files with 1,424 assertions including 122 focused VIP assertions, both images, and all four WooCommerce runtimes.
- M05-S04 is complete. Strict earned-date policy, immutable per-version materialization, bounded single-flight lifecycle processing, mixed-version-safe immutable expiry, nearest-relevant 30/14/7 reminder fences, original-lot reservation restoration, and minimized liability preview passed exact-head run `31756142529`: clean 34-migration replay, all 32 pgTAP files with 1,472 assertions including 48 focused expiry assertions, both concurrency probes, both images, and all four WooCommerce runtimes. A production-build Playwright fixture exercised the real merchant shell and expiry editor at 1440 by 1000 and 390 by 844, including 21-day reminder filtering, liability evidence, mobile navigation, zero horizontal overflow, and no browser diagnostics.
- M05-S05 is complete. Bigint-safe tenant/customer progression, immutable history, exact next/retention/re-entry milestones, aggregate tier performance, advanced policy authoring/simulation, and responsive merchant/customer experiences passed exact-head run `31759304542`: clean 35-migration replay, all 32 pgTAP files with 1,491 assertions including 141 focused VIP assertions, both concurrency probes, both images, and all four WooCommerce runtimes. Desktop/mobile production-build interaction had no overflow or component diagnostics.
- M05-S06 predeployment verification is exact-head green. The shadow loop found that migrated Bloom/Icon could display 6/7 points while executing the Rose 5-point base rate; no production value moved because advanced VIP remains undeployed and unpublished. Contracts, PostgreSQL, migration, and editor now bind displayed rate to the exact executable multiplier, all 36 V1/V2 Rose/Bloom/Icon award comparisons match, and run `31760806620` passed a clean 36-migration replay, all 32 pgTAP files with 1,494 assertions, both concurrency probes, both images, and all four WooCommerce runtimes. S06 now has a 48-check, four-approval, nine-artifact fail-closed canary validator whose exact semantic schemas bind approved qualification/lifecycle/benefit/override/expiry/reminder policies and ceilings, released images/plugin/contracts, all shadow/qualification/movement/benefit/expiry/progression evidence and counts, zero-difference reconciliation, canonical chronology, rollback after canary end, and at least 24 hours of covering observation. Integrated candidate `4d04ed7` passed all seven CI jobs in run `33122704264` and all three Security jobs in run `33122704267`. Thirteen controls pass, 35 approved production controls remain pending, and operability 3/10 blocks close. Approved release, pilot store, recovery point, disabled deployment, Starfiniti-only canary, reconciliation, rollback, observation, and final scoring remain open; production was unchanged.
- M06-S01 through S05 are repository-complete. Opaque first attribution, historical paid/new-customer qualification and cooling, atomic two-sided give-get ledger value and refund compensation, reason-bound minimized fraud review, bounded recovery, and customer/merchant referral experiences passed exact-head database, concurrency, browser, privacy, checkout-independence, and WooCommerce gates. S06 now has a 48-check, four-approval, nine-artifact fail-closed canary validator whose exact semantic schemas bind approved attribution/cooling/fraud/value/retention policies and ceilings, released images/plugin/contracts, all referral behavior/value/review/recovery evidence and counts, zero-difference reconciliation, canonical chronology, rollback after canary end, and at least 24 hours of covering observation. Integrated head `c38c658` passed all seven CI jobs in run `33121739492` and all three Security jobs in run `33121739491`. Fourteen controls pass, 34 approved production controls remain pending, and operability 3/10 blocks close. Approved release, pilot store, recovery point, disabled deployment, Starfiniti-only canary, full reconciliation, rollback, observation, and final scoring remain open; production was unchanged.
- M06-S01 through S05 are exact-head green on draft PR #31. Strict policy, signed offline capture, first attribution, historical qualification/cooling, two-sided exactly-once issuance/compensation, Auth-derived reversible fraud review, customer sharing/progress/history, and a fact-sourced merchant funnel passed runs `31763563259`, `31764805380`, `31766887239`, `31768294674`, and `31770764870`; the latest passed baseline, both images, a clean 41-migration replay, all 35 pgTAP files with 1,700 assertions including 151 focused referral assertions, all three concurrency probes, 126 dashboard tests, 136 contract tests, 14 accepted ADRs, and all four WooCommerce runtimes. Production-build desktop/mobile browser review passed keyboard, landmarks, copy/share fallback, navigation, overflow containment, outage isolation, and zero diagnostics. M06-S06 deployment/canary closeout is active.
- M07-S01 is exact-head green on draft PR #32. Strict canonical-fact audience predicates, immutable versions, database-timed bounded private snapshots, tenant-derived idempotent commands, and rollback-safe entitlement behavior under ADR-0021 passed run `31773939480` at `8192dd7`: the complete repository gate, both production images, a clean 42-migration replay, all 36 pgTAP files with 1,770 assertions including 70 focused audience assertions, all four concurrency probes, 126 dashboard tests, 21 worker tests, 143 contract tests, 53 domain tests, 15 accepted ADRs, and all four WooCommerce runtimes. The two-session audience race reconciled to one snapshot/member/audit. Adversarial security, tenancy, correctness, complexity, idiom, and cruft review found and resolved every blocker and should-fix issue.
- M07-S02 is exact-head green on draft PR #32. Strict contracts for seven campaign behaviors, explicit-offset/IANA/local schedule evidence, hard effect/points/liability ceilings, exact decimal RPC values, immutable inclusion/exclusion bindings, exact salted-hash treatment/control assignment, and Auth-derived draft/preview/approve/pause/cancel commands under ADR-0022 passed run `31777177961` at `e92f52d`: the complete repository gate, both production images, a clean 43-migration replay, all 37 pgTAP files with 1,851 assertions including 81 focused campaign assertions, all four concurrency probes, 126 dashboard tests, 21 worker tests, 157 contract tests, 53 domain tests, 16 accepted ADRs, and all four WooCommerce runtimes.
- M07-S03 is exact-head green on draft PR #32. Stable operation/member/campaign locking, private global/member/points/liability counters, pure fixed-bonus and highest-priority multiplier evaluation, independent PostgreSQL reconstruction, atomic programme plus campaign-attributed ledger commits, immutable replay context, and non-purchase capacity reservations under ADR-0023 passed run `32662380030` at `c82f3ed`: baseline, both images, a clean 44-migration replay, all 38 pgTAP files with 1,914 assertions including 62 focused capacity assertions, all four concurrency probes, 126 dashboard tests, 22 worker tests, 160 contract tests, 57 domain tests, 17 ADRs, and all four WooCommerce runtimes. The campaign race proved exactly one last-budget reservation and reconciled counters; S04 now extends this boundary as recorded below.
- M07-S04 is exact-head green on draft PR #32. ADR-0024 binds purchase context and canonical milestone/win-back/tier/referral/limited work to one exact programme, keeps private leased jobs database-authoritative, caps recovery at ten attempts/manual review, commits point or campaign-funded native reward value atomically, and preserves accepted/reversal work when rollout is disabled. Run `32667482144` at `0b60d1b` passed baseline, both images, a clean 45-migration replay, all 38 pgTAP files with 1,967 assertions including 115 focused campaign trigger assertions, all five concurrency probes, 126 dashboard tests, 25 worker tests, 167 contract tests, 57 domain tests, 17 validator-tracked ADRs, and all four WooCommerce runtimes. Direct private-queue access remains denied to the worker; no production schedule, entitlement canary, or campaign value is live. S05 merchant authoring/calendar/results is active.
- M07-S05 is exact-head green on draft PR #32. The responsive Hub-style command center supports allowlisted multi-condition audiences, immutable editing, publication and frozen snapshots, all seven campaign behaviors, arbitrary valid IANA schedules with DST rejection, effect/member/points/liability/control ceilings, explicit preview confirmation, calendar and lifecycle operations, and independent exact results. ADR-0025 limits the Auth-derived projection to public metadata and aggregates, keeps identities/assignments/sources/errors private, and labels attribution as influenced rather than experimentally incremental. Run `32671197966` at `fcb1f3c` passed baseline, both images, a clean 46-migration replay, all 38 pgTAP files with 1,975 assertions including 123 focused campaign assertions, all five concurrency probes, 139 dashboard tests, 25 worker tests, 170 contract tests, 57 domain tests, 19 ADRs, and all four WooCommerce runtimes. Desktop/mobile production-build browser review passed keyboard, navigation, dynamic builder, dark mode, overflow, and zero-diagnostic checks. S06 reviewed merge/deployment/canary/reconciliation/scoring is active; no production campaign schedule, entitlement canary, or value is live.
- M07-S06 adversarial review found that ordinary refunds omitted separately posted purchase-campaign points, merchant-entered native liability could understate the referenced reward, one audience snapshot could mix separately committed source states under `READ COMMITTED`, accepted campaigns had no activation/completion path, delayed trigger execution extended point lifetime, deterministic trigger failures retried ten times per member, and hard-coded/free-text selector codes could author silent no-op campaigns. ADR-0026 adds cumulative per-effect compensation and atomic worker rollback. ADR-0027 binds hard monetary ceilings to exact published fixed-discount face value and rejects unsupported/unpublished/cross-programme rewards. ADR-0028 evaluates the bounded candidate set and every decision in one materialized PostgreSQL statement snapshot. Run `32674872811` passed the complete repository/database/container/WooCommerce gate for those three fixes. ADR-0029's bounded database-timed lifecycle passed run `32675181455` with 2,007 pgTAP assertions, and canonical trigger-time expiry passed run `32675527696` with 2,008 assertions plus every repository/runtime gate. Native connector preflight and deterministic/manual-review classification passed exact-head run `32676129608` with 2,010 pgTAP assertions and every repository/runtime gate. ADR-0030 projects selectors from the exact published programme, rejects unknown direct-RPC selectors, blocks incompatible programme publication, and serializes campaign approval/publication on the exact stable programme row. Exact-head run `32677551229` at `a9e75f2` passed the complete baseline, both images, clean 47-migration replay, all 38 pgTAP files with 2,016 assertions including 160 focused campaign assertions, the two-session selector race, all five concurrency probes, and all four WooCommerce runtimes. Remaining release findings, deployment, and canary are pending; production campaign rollout remains paused.
- M07-S06 pre-canary automation is active at an honest 90/100. The hardened gate requires 51 checks, four synchronized approvals, nine unique path/digest/semantic-bound production artifacts, exact approved-policy/ceiling and release/plugin/contract bindings, audience/behavior/concurrency/refund/native-reward evidence and count reconciliation, zero-difference value/privacy/queue states, canonical chronology, rollback after canary end, and at least 24 hours of covering observation. Exact integrated head `6fb98ad` passed CI run `33120192619` on attempt 2 and Security run `33120192581`; the first container attempt ended only on npm registry `ECONNRESET`, and its isolated retry passed. Fourteen controls pass and 37 approved production controls remain pending; operability remains 3/10 and blocks close. Production remains unchanged.
- The M07 merchant-results release audit also found that the table discarded most of the protected capacity, reversal, trigger-outcome, and queue evidence returned by PostgreSQL. A native keyboard-operable disclosure now exposes those exact aggregates; a focused real-component browser fixture passed at 1440×1000 and 390×844 with no page overflow or browser diagnostics. Exact-head run `32679145086` at `1b1406b` passed baseline, database, both images, and all four WooCommerce runtime lanes.
- M08-S01 is exact-head green on draft PR #32. ADR-0031 and `NotificationEventV1` define nine strict English-only provider-neutral event types with exact bigint/instant/resource payloads and no arbitrary PII, coupon, secret, provider-response, or ledger fields. PostgreSQL owns immutable deduplication, Auth-derived transactional/marketing customer preferences, stronger provider/system suppression, privacy-erasure suppression, and point-expiry event creation. Run `32682221777` at `33e0396` passed the complete baseline, both images, a clean 48-migration replay, all 39 pgTAP files with 2,066 assertions including 50 focused notification assertions, every concurrency probe, and all four WooCommerce runtimes after retrying one upstream package-download 504. No SMTP, Klaviyo, webhook, contact copy, or production notification delivery is active.
- M08-S02 is exact-head green on draft PR #32. ADR-0032, strict dispatch contracts, a private database-owned lease/attempt boundary, six immutable English templates, and a separate optional SMTP worker provide late verified-Auth contact resolution, dispatch-time entitlement/consent/suppression checks, deterministic Message-ID, bounded safe retries, terminal handling for deterministic local message failures, and manual review for ambiguous remote acceptance. Run `32686442063` at `604bbeb` passed the complete baseline, both production images, a clean 49-migration replay, all 40 pgTAP files with 2,152 assertions including all 86 focused SMTP assertions, every concurrency probe, and all four WooCommerce runtimes. The 46 worker tests include 16 focused SMTP tests and a real loopback sink. The Compose profile remains disabled and no production email is active.
- M08-S03 is exact-head green on draft PR #32. ADR-0033, strict projection contracts, tenant/API-key fingerprint binding, late verified-Auth contact resolution, pinned Klaviyo API revision `2026-07-15`, provider-suppression import, and a separate optional worker provide minimized profiles/events, dispatch-time managed entitlement and consent checks, safe event/unsubscribe retries, and manual review for ambiguous opt-in. Run `32689107286` at `a6bbf14` passed the complete baseline, both production images, a clean 50-migration replay, all 41 pgTAP files with 2,219 assertions including all 67 focused Klaviyo assertions, every concurrency probe, and all four WooCommerce runtimes. The Compose profile remains disabled with no production connection, key, or delivery.
- M08-S04 is exact-head green on draft PR #32. ADR-0034, strict delivery contracts, endpoint-bound current/previous secret fingerprints, Standard Webhooks v1 exact-body HMAC signatures, public-only DNS resolution with socket pinning, no redirects, dispatch-time subscription/entitlement/consent checks, bounded payloads/responses/rate windows, and ten-attempt manual review provide a replay-safe generic escape hatch without exposing contacts or secret material. Run `32691991986` at `ea9aa00` passed the complete baseline, both production images, a clean 51-migration replay, all 42 pgTAP files with 2,277 assertions including all 58 focused webhook assertions, every concurrency probe, and all four WooCommerce runtimes. The 39 focused worker tests use a real loopback sink and the 40 focused contract tests cover the strict boundary. The Compose profile remains disabled with no production endpoint, subscription, secret, or delivery; M08-S05 merchant template and delivery-health experience is active.
- M08-S05 is exact-head green on draft PR #32. ADR-0035, immutable tenant English template versions, private active bindings, owner/admin Auth authority, exact event-token allowlists, deterministic escaped HTML, actor-bound SMTP tests, and a minimized consent/provider/issue projection provide safe merchant control without persisting contacts or exposing secrets. Run `32836814262` at `a377ef7` passed the complete baseline, both production images, a clean 54-migration replay, all 43 pgTAP files with 2,340 assertions including all 63 focused template/health assertions, every concurrency probe, and all four WooCommerce runtimes. The real-component Playwright fixture passed template switching, safe preview, test action visibility, dark theme, mobile navigation, and zero browser diagnostics. No production provider is active; M08-S06 disabled deployment, canary, reconciliation, rollback, and scoring is active.
- M08-S06 pre-canary automation is active at an honest 90/100. The hardened gate requires 53 checks, four synchronized approvals, nine unique path/digest/semantic-bound production artifacts, exact approved-policy/ceiling and release-adapter bindings, provider-evidence and count reconciliation, zero-difference value/privacy/queue states, canonical chronology, rollback after canary end, and at least 24 hours of covering observation. Fifteen controls pass and 38 approved production controls remain pending; operability remains 3/10 and blocks close until credentials, approved disabled deployment, bounded SMTP/Klaviyo/webhook canaries, outage/value/checkout proof, reconciliation, rollback, and observation pass. Production remains unchanged while exact-head integration CI is refreshed.
- M09-S01 through S04 are complete on draft PR #36. The strict Auth-derived hosted experience and Hub-style seven-area member/guest journeys passed desktop, mobile, and 200%-reflow review. ADR-0037 adds signed demand-driven PII-free monotonic WooCommerce snapshots and local classic placements; ADR-0038 adds one official namespaced Store API projection and a separately flagged, budgeted Blocks panel without value authority or a live Hub dependency. Exact-head run `32859649418` at `bf5ec90` passed baseline, both images, a clean 56-migration replay, all 44 pgTAP files with 2,392 assertions, and all four minimum/current HPOS/legacy WooCommerce runtimes, including staged flags, real Store API responses, native coupons, no-script fallback, and forced Hub failure. Real-browser desktop/mobile review passed exact JavaScript/CSS budgets, focus, fresh/stale states, unsafe-link rejection, overflow, and zero diagnostics. M09-S05 branding, accessibility, and outage hardening is active.
- M09-S05 is complete on draft PR #36. ADR-0039 adds a strict English-only V2 presentation, exact seven-section composition, reviewed Lucide asset keys, controlled density/tokens, an English-only selector-minimized save wrapper, Auth-derived and anonymous V2 reads, RPC-absence-only V1 rollout compatibility, and bounded hosted/public recovery states. Exact-head run `32875015095` at `4f8be7a` passed the complete baseline, both production images, a clean 57-migration replay, all 44 pgTAP files with 2,441 assertions, every concurrency probe, and all four minimum/current HPOS/legacy WooCommerce runtime cells. Chrome review at 1512 × 982, 390 × 844, 320 pixels, and 200% scale passed the approved Hub comparison, interactive member/public/WooCommerce states, English-only DOM, anonymous-public privacy, visible keyboard focus, reduced motion, no horizontal overflow, and zero final diagnostics. The audit found and repaired dark-theme contrast before final evidence capture. M09-S06 disabled deployment, Starfiniti hosted/WooCommerce canary, reconciliation, rollback, and scoring is active.
- M09-S06 pre-canary automation is active at an honest 88/100. Integrated candidate `c989229` passed CI run `33100009132` and Security run `33100009100`; public health/login and unauthorized Auth/REST baselines pass, and approved read-only Proxmox access confirms both VMs are running. The hardened gate now requires 34 checks, four synchronized approvals, nine unique path/digest/semantic-bound production artifacts, exact top-level and artifact schemas, approval/release/budget/surface/outage/reconciliation/rollback/observation bindings, canonical chronology, fixed automatic failures, exact score/category floors, and positive/adversarial completion fixtures. Schema drift, future-dated evidence, cyclic structures, oversized text, and invalid task graphs fail deterministically. Eight controls pass and 26 approved production controls remain pending; operability remains 4/10 and blocks close. No production mutation was attempted: PR #57 is now ready and no real WooCommerce pilot/window is approved.
- M01 live read-only reconstruction confirms zero commerce connections, customers, wallets, ledger transactions, reservations, events, effects, or commands. The real-store pilot cannot accidentally overlap existing accepted value.
- `npm run pilot:validate` enforces 22 M01 checks. Database WAL recovery is proven. The later tar-over-stdin transfer-amplification incident was contained after 3.6 TB of internal VM-to-host traffic and replaced with a restricted incremental rsync stage; measured manual warm-cache and timer archives passed without stopping PostgreSQL or local recovery. Twenty-one store/application/alert/reconciliation checks remain pending; application/Auth/signing recovery is not claimed.

## What works

- The dashboard can initiate PKCE sign-in through the exact Supabase provider `custom:starfiniti-sso`, accepts only the configured Supabase `/auth/v1/authorize` destination, and returns through the existing private no-store callback to the same Supabase Auth/RLS session model. Customer password flows remain separate.
- Phase 0's reproducible npm workspace, responsive standalone Next.js Overview, Supabase migration baseline, WooCommerce HPOS scaffold, repository operating system, and pinned CI are verified.
- The Docker-backed GitHub Actions database job replays migrations/seed and runs 1,095 transactional pgTAP assertions plus real two-session ledger/programme concurrency and property probes.
- Phase 1 Rosy Rewards semantics are owner-approved and encoded as versioned configuration rather than global merchant assumptions.
- Pure domain behavior covers integer award calculation, explicit historical tier snapshots, 30-day release, 12-month rolling expiry, earliest-expiry redemption ordering, rolling-spend tiers with grace, cumulative original-attribution refund reversal, and negative balances.
- The current integration head passes 983 workspace tests: 411 dashboard, 118 worker,
  361 versioned-contract, 85 domain, and 8 TypeScript SDK tests.
- The platform carries a full AGPL-3.0 license and package metadata; the WooCommerce connector remains independently GPL-2.0-or-later.
- Phase 2 architecture is complete and deterministically validated: tenant/Auth trust, identity, double-entry ledger, signed inbox/outbox, reward reservation, privacy, backup/restore, deployment, and SLO models are reviewable.
- Phase 3 tenancy/RLS is complete: organizations, memberships, workspaces, programme groups, support grants, least-privilege roles, composite tenant keys, and live authorization policies execute successfully in disposable Supabase CI.
- Phase 4 commerce ingestion is complete: WooCommerce local outbox, Action Scheduler retry/dead-letter behavior, raw-body HMAC receiver, durable inbox, canonical normalization, effect fences, and transactional outbox are execution-verified.
- Phase 5 ledger is complete: immutable zero-sum transactions/entries, wallets/accounts, FIFO lots, compensating allocations, projections/rebuilds, value commands, export, and liability reporting are execution-verified.
- Phase 6 programme execution is complete: deterministic conditional earning/exclusions, immutable publication/scheduling, materialized tiers/rewards, live/simulation evidence, effective tier intervals, reward failure compensation, and advance expiry notifications are execution-verified.
- Phase 7 implements a separately credentialed WooCommerce effect worker, completed-order awards, cumulative original-attribution refund reversal, channel-ID customer resolution, native issue/cancel commands, PII-free coupon capture, expiry compensation, connection health watermarks, source reconciliation, and an installable GPL plugin artifact.
- The plugin encrypts its signing key at rest, declares HPOS support, keeps checkout independent of the hub, exposes queue/dead-letter diagnostics, and provides customer loyalty, privacy export/erase, and WP-CLI recovery surfaces.
- All 43 WooCommerce strings use the plugin text domain with exact POT coverage. The initial package is English-only, retains the standard WordPress translation boundary for future catalogs, and tests setup-package import in each minimum/current HPOS/legacy runtime.
- The supported WooCommerce smoke matrix passes on WordPress 6.6.5/WooCommerce 9.0.2/PHP 8.1 and WordPress 7.0.2/WooCommerce 10.9.4/PHP 8.3, each with HPOS and legacy storage. It executes classic and Blocks coupon paths under hub outage, order completion/capture, partial/full refunds, reconciliation, activation lifecycle, and dead-letter recovery.
- WooCommerce account/cart loyalty rendering has an enforced 0-byte connector JavaScript, 0-byte connector CSS, and zero hub-request budget. Account results are capped at 20 rewards, markup sizes are bounded in the real runtime matrix, and checkout continues to use native WooCommerce coupon behavior.
- The Next.js merchant shell now verifies Supabase Auth claims, refreshes sessions through the Next.js 16 request proxy, derives live organization/workspace/programme scope through the authenticated Data API and RLS, handles unassigned users safely, and provides sign-in/sign-out/PKCE callback paths without exposing a secret key.
- The merchant programme surface provides structured tier/reward editing, deterministic earning preview, contract validation, new immutable draft versions, exact-fingerprint publish/schedule confirmation, role-aware controls, version history, and tenant-visible administration audit evidence.
- Overview, programme onboarding/editor/history, connector operations, customer administration, and experience administration render English system copy with no language selector. Legacy locale query parameters are discarded. Programme publication and customer credit-expiry inputs still interpret Europe/Ljubljana wall time deterministically across server time zones; DST gaps and ambiguous wall times fail closed.
- Existing tenant owners/admins can create the first programme in their selected active programme group through a server-derived, idempotent database command with immutable audit evidence; public tenant provisioning stays disabled.
- Customer operations provide bounded literal display-reference search, database-masked channel identity, exact text-form pending/available/reserved/spent/expired/reversed buckets, and the latest immutable ledger entries with programme-version and correlation attribution.
- Every rendered merchant route exposes a first-focus English skip link to one focusable main landmark; global visible focus includes text areas, reduced-motion behavior is guarded, and the sign-in card remains within a 390-pixel viewport.
- The operations view can download a versioned, tenant-scoped support bundle containing only public scope/connection IDs, queue totals, watermarks, and a labelled bounded sample of grouped canonical error codes. Item IDs, display names, payloads, commerce/customer identities, actors, reasons, signing references, and secrets are omitted.
- A tenant owner/admin can provision the first WooCommerce connector for an active workspace and published programme from the operations view. The server consumes one unique reference from a root-readable deployment key pool, records a secret-free immutable audit event, and returns an exact setup package only in that action result; the plugin validates and encrypts the imported signing key at rest.
- A deployment operator can create the initial organization, live owner membership, workspace, programme group, and link for an existing Auth UUID through one atomic repository command. Exact retries are stable, changed retries fail closed, audit metadata is minimized, and browser, dashboard-runtime, and worker roles cannot execute the boundary.
- Pull-request CI builds the dashboard and worker Dockerfiles from a constrained context using a digest-pinned Node base. Release `v0.1.11` publishes commit-addressed GHCR dashboard/worker images and the checksummed WooCommerce ZIP after the full release/database gate passed.
- `npm run deploy:preflight -- --env <absolute-secret-env-file>` validates the populated Proxmox application configuration and signing-key pool without printing values. It covers Compose parity, placeholders, immutable image selectors, HTTPS origins, dedicated database logins, pool schema, and Linux owner-only permissions; network and live-role checks remain deployment-time gates.
- The application Compose contract maps the public Supabase TLS hostname to the private reverse-proxy address inside the dashboard container, avoiding public-NAT hairpin failures without changing browser URLs or certificate validation. Production PostgREST exposes `public,graphql_public,loyalty`; explicit grants and tenant RLS remain authoritative.
- `/api/healthz` returns only `ok` or `unavailable` with no-store headers and fails closed unless the runtime login can execute the exact commerce-ingestion and connector-provisioning functions and the mounted signing pool has at least one valid entry. Proxmox Compose uses this dependency-aware route for dashboard readiness.
- The customer-experience view reads one RLS-scoped workspace/programme-group theme and previews English member/guest states responsively. Owners/admins can revision a contract-validated token set and bounded English customer copy through idempotent audited commands; inaccessible colors, arbitrary CSS, remote fonts, scripts, uploads, and caller-supplied tenant authority are rejected. Legacy translation rows remain isolated and non-selectable.
- A public mobile-first hosted loyalty route renders one active published programme in English using approved theme tokens, tier rates, and reward names/costs. Its bounded anonymous read model omits organization identity, customer/ledger state, raw programme/reward configuration, audit, integration, and commerce evidence; suspended or mixed-scope resources return no document.
- A logged-in WooCommerce customer can open a locally generated five-minute HMAC link, authenticate to the hub, review the exact store, and explicitly confirm one Auth/customer link. Nonces and proofs are one-use and retained only as hashes; customer IDs are channel-bound, conflicts fail closed, and email is never a merge key.
- The authenticated `/account/loyalty` route derives every account from the live Auth/customer link and exposes exact text-form balances, current tier, next expiry, up to 20 safe rewards, active reservations, and ten redacted ledger activities. It accepts no caller-supplied tenant/customer scope and exposes no actors, source references, request evidence, metadata, private profile text, or other identities.
- Hosted login, WooCommerce claim, member-account, and redemption routes render English system copy. Legacy locale parameters are removed from safe navigation, and connector claim links contain no language field, leaving signed identity authority unchanged.
- A hosted customer can enter their password again and download one versioned JSON document containing their Auth email, active linked store identities, wallets, tiers, reservations, and complete wallet-side ledger. A server-issued capability is stored only as a hash, expires after five minutes, is bound to the exact Auth subject and Supabase session, and is atomically consumed once; the private response is not persisted and each included customer receives immutable audit evidence.
- Customer detail now includes minimized current versus qualified tier, transition, exact eligible-spend minor units, and effective/grace timestamps without exposing private decision explanations or command evidence.
- Customer detail activity can be filtered into orders/refunds, rewards, release/expiry, and adjustments with visible bounded-result counts; unknown or array query input falls back to all activity and never changes the underlying RLS read.
- Connector operations provide tenant-authorized health/queue counts, bounded failure metadata without private payloads, and audited owner/admin/operator replay of dead-letter canonical effects. Outbound coupon dead letters remain inspect-only because points compensation may already exist.
- Customer owners/admins can preview and confirm signed whole-point adjustments against an exact text-form balance. Credits require expiry, removals show a strong negative-balance warning, and every result is one reason-bound immutable double-entry transaction plus administration audit evidence.
- Customer owners/admins can select 2–50 active wallets for one uniform bulk credit/debit, obtain a read-only canonical dry run with exact projected balances, and approve its fingerprint. Execution locks balances deterministically and atomically appends one immutable ledger effect per customer plus immutable batch/item and aggregate audit evidence; exact retries are safe and stale previews fail closed.
- Connector owners/admins/operators can review and queue one WooCommerce order reconciliation through an audited private outbox and signed polling route. The plugin re-emits stable source facts idempotently, never edits points directly, and terminates missing orders explicitly.
- Public WooCommerce event and command routes enforce their 64 KiB limit while streaming before any database or signing-material access, so omitted or chunked `Content-Length` cannot force unbounded unauthenticated buffering.
- WooCommerce issue, cancellation, and reconciliation commands stop after ten claims in a visible inspect-only manual-review state. Ambiguous coupon outcomes retain the reservation and ledger state until the native result is verified; they never trigger speculative compensation.
- Native percentage rewards support 0.01–100% without a maximum cap. The contract/editor reject caps, PostgreSQL independently blocks direct-RPC publication/scheduling and legacy capped redemption before value moves, and the plugin retains a defensive rejection boundary.
- The Overview no longer renders illustrative tenant figures. It reads versioned, tenant/workspace/programme-authorized aggregates for members, eligible loyalty spend, repeat-member rate, captured-to-awarded point redemption, point liability, and bounded 7/30/90-day trends without exposing private source evidence.
- Merchant Overview, programme, customer, connector, and experience routes now share the owner-selected Hub-style application shell with the real Starfiniti mark, grouped Lucide navigation, working mobile drawer, persistent theme and refresh controls, and no tenant or language switcher. Overview is a data-backed launch command center whose checklist, performance rail, and activity rows derive from live programme, connector, reporting, and audit state. The exact `v0.1.11` images are live on Proxmox.
- Programme administration is live as four distinct merchant workflows. Overview shows readiness, configuration summaries, immutable version actions, and recent audit evidence; Earning rules provides the complete V2 source/condition/cap builder and exact simulation; Rewards provides a guided WooCommerce-ready catalogue; VIP tiers provides a separate ladder with overlap blocking and qualification preview. Focused edits keep the other programme fields intact in the same immutable draft contract.
- The completed V2 Earning Rules workflow provides source templates, allowlisted product/category/currency/market/channel/segment/tier/date conditions, explicit commerce exclusions, event/member-period caps, conflict warnings, and the exact worker evaluator. Production canary authoring and simulation pass while the published V1 remains active until M05 supplies an equivalent tier-rate migration.

## Partial

- Phases 0 through 7 are complete for the active WooCommerce scope. Shopify Phase 8 is deferred by product-owner direction.
- Phase 9 code, English-only releases, self-hosted production infrastructure, Authentik-to-Supabase SSO, verified owner login, atomic Starfiniti tenant bootstrap, and the focused Programme workflows are live. Public signup remains disabled; one real WooCommerce store connection is pending before tenant value is accepted.

## Broken or unavailable

- Docker, Podman, and WSL remain unavailable on this workstation; GitHub Actions is the verified database runner.
- The production dashboard and API are live at `loyalty.starfiniti.com` and `api.loyalty.starfiniti.com`; the verified owner tenant is connected, while a real WooCommerce store is not yet connected.
- An isolated database recovered from the encrypted off-host Borg archive, replayed archived WAL, promoted cleanly, and became ready in 9 seconds with all twenty-six migrations. Full application/Auth/secret-escrow recovery smoke remains due.
- The globally configured `pnpm` shim is broken; npm is the supported package manager.

## Database migration state

The active integration branch validates 87 versioned migrations and 69 pgTAP files.
Exact code-and-evidence head `8c7da2f65dea0e677e1110244393e7b90b6f11bd` passed CI
`33176416306`, Security `33176416320`, and external CodeQL: clean replay of
all 86 migrations, all 68 pgTAP files with 3,772 assertions, all 22 concurrency
probes, both production images, and all four WooCommerce runtime variants. The
current PR-head rollup remains authoritative for documentation-only successors.
Production has applied twenty-eight migrations through `20260813200000`, contains
the audited initial Starfiniti owner scope and explicit `programme.v2` canary, and
still has no customer or loyalty-ledger value.

## Git state

Public repository `Starfiniti/starfiniti-loyalty`; PR `#6` merged the Phase 7 WooCommerce pipeline, PR `#7` merged Phase 9, PR `#9` merged Authentik workforce SSO, PR `#10` merged the English-only launch presentation, PR `#17` merged the Hub-style redesign, PR `#20` merged the focused Programme workflows, PR `#26` merged M03, and PR `#27` versioned the production-backup repair. Release `v0.1.11` runs the exact M03/recovery commit in production. GitHub recognizes the repository license as GNU AGPLv3.

The active implementation branch is `codex/enterprise-roadmap-integration`; ready PR
#57 targets `main`, consolidates M04–M16, and is merge-clean. Exact code-and-evidence
head `2aedc97747dc38a8dfcdf745fb9ae3085f52d28e` passed all twelve PR checks; the current PR-head rollup remains the
handoff authority. Stale Phase 4 local planning changes remain preserved as
`preserve stale phase-4 planning state before enterprise roadmap 2026-08-13` and are
not part of this branch.

## Last verification

- Exact code-and-evidence head `2aedc97747dc38a8dfcdf745fb9ae3085f52d28e`
  passed CI `33183341956`, Security `33183341957`, and external CodeQL `98890257326` with all
  twelve PR checks green; ready PR #57 is merge-clean against `main`. The database
  job replayed 86 migrations, passed all 68 pgTAP files with 3,773 assertions and
  all 22 concurrency probes; both images and all four WooCommerce runtimes passed.
- M13 credential-boundary commit `945c92f` independently passed CI `33126151633`, Security `33126151639`, and external CodeQL before the M01 evidence refresh was added.
- `npm run ci:validate` — passed locally for four baseline jobs, three security jobs, release SBOM/provenance, immutable action inputs, and container boundaries.
- `npm run security:validate` — passed its corruption matrix for 25 security checks and the isolated six-job ZAP plan; 15 exact repository/execution controls pass and ten external/final-review gates remain deliberately non-passing.
- `npm run recovery:validate` — passed its corruption and controller self-tests for 32 checks and fourteen exact stages; seven repository controls pass and 25 environment/exercise/reconciliation gates remain deliberately non-passing.
- `npm run operations:validate` — passed the 35-check, 31-signal, 27-alert contract, positive completion case, and missing-series, off-site-recovery, rule-drift, paging-delay, mutable-dashboard, unsafe-path, reused-exercise, false-completion, and approval corruption cases.
- `npm run ga:validate` — passed the 50-check contract, valid synthetic closeout, and false-completion, duration, continuity, drift, reconciliation, finding, score, claims, artifact, module, and approval corruption cases; five repository checks pass and 45 live/module/approval checks remain deliberately pending.
- `npm audit --audit-level=high` — after upgrading `@wordpress/env` 11.8.0 to 11.14.0, passed with zero complete-tree vulnerabilities; `npm ls` confirms patched `adm-zip` 0.6.0 and no `extract-zip`. Exact-head CI run `33057428413` passed all four Linux WooCommerce cells and closed R-032.

- Clean `origin/main` baseline on Windows reached `npm run format:check` and reproduced the tracked CRLF checkout warning across 180 files; no project lint, type, test, or build step ran in that invocation. Targeted changed-file formatting and independent gates are required for M00 rather than rewriting unrelated history.
- M00 clean `npm ci` passed with 962 installed packages. Lint, all workspace typechecks, 177 unit tests, dashboard and worker production builds, four workflow jobs, deployment self-tests, eight architecture models/five ADRs, fourteen accessibility surfaces, WooCommerce source/localization/storefront budgets, twenty-six migration files, secret scan, zero-vulnerability production audit, and licence validation passed.
- M00 changed-file Prettier, YAML/JSON parse, unique dependency graph, one-active-module invariant, product-score reconciliation, and `git diff --check` passed. The full development audit retains R-032 for the pinned WordPress runtime ZIP library; production dependencies remain clean.

- `npm run test --workspace=@starfiniti/domain` — passed with 24 tests.
- `npm run typecheck --workspace=@starfiniti/domain` — passed.
- Exact-head Programme verification passes repository lint/typecheck/build, all 177 tests, the 14-surface accessibility guard, CI/deployment/architecture/database/WooCommerce validators, secret scan, production dependency audit, and license validation.
- PR `#20` exact-head run `31713151458` passed all seven jobs with both production images, twenty-six migration replays, all 1,049 pgTAP assertions, concurrency/property probes, and all four minimum/current HPOS/legacy WooCommerce runtimes.
- Release run `31713458344` published `v0.1.10`, the checksummed WooCommerce ZIP, and immutable commit `4713c65e4ca47c0a97264854afea46f6a8730a3a` dashboard/worker images after the full release gate passed.
- `npm run db:validate` — passed for twenty-six migrations, Supabase config, and twenty-five transactional pgTAP files.
- `npm run secrets:scan` — passed with no findings.
- `npm run audit:prod` — passed with zero production vulnerabilities.
- `npm run licenses` — passed for six AGPL npm package declarations, the full AGPL text, and both WooCommerce GPL declarations.
- Hub-style design verification passed at 1440 × 1024 and 390 × 844 with working drawer/disclosure/theme interactions, no horizontal mobile overflow, and `design-qa.md` final result `passed`.
- Focused Programme Chrome QA passed at 1440 × 1024 and 390 × 844 with no horizontal overflow; first-reward creation, populated reward editing, dynamic `€200 → Bloom → 1,200 points` simulation, threshold-overlap blocking, exact active navigation, and mobile drawer behavior all passed without console warnings or errors.
- PR `#17` exact-head run `31703869751` and PR `#18` exact-head run `31704901506` each passed all seven jobs, including the 1,049-assertion database gate, both production containers, and all four minimum/current HPOS/legacy WooCommerce runtimes.
- Release run `31705154056` published `v0.1.9`, the checksummed WooCommerce ZIP, and immutable commit `8d27d091d7a48094dbbb1a45bd37925a092d88b3` dashboard/worker images after the full release gate passed.
- `npm run architecture:validate` — passed for eight Phase 2 models and four accepted ADRs; it now runs inside `npm run check`.
- PR exact-head run `31645976689` passed all seven release-hardening jobs with 164 unit tests, twenty-six migration replays, all 1,049 pgTAP assertions, concurrency/property probes, both production images, and all four uncapped-percentage WooCommerce runtime variants.
- Release run `31681490618` passed the full baseline, twenty-six-migration/1,049-pgTAP database gate, image publication, WooCommerce packaging, and GitHub release publication for `v0.1.0`.
- PR `#10` exact-head run `31691207685` passed all seven jobs with 166 unit tests, twenty-six migration replays, all 1,049 pgTAP assertions, both production images, and all four minimum/current HPOS/legacy WooCommerce runtimes.
- Release run `31691454507` published the checksummed English-only WooCommerce ZIP and immutable dashboard/worker images for `v0.1.2` after the full release and database gate passed.
- Live production smoke on 2026-08-13 passed TLS, dashboard readiness, English-only login and legacy-locale fallback with no switcher, Starfiniti SSO entry, unsigned Woo endpoint rejection, public database-port isolation, all eleven Supabase services, both immutable application images, WAL/base backup timers, and off-host physical-backup verification.
- Live Chrome verification on 2026-08-13 completed Authentik custom OIDC and Supabase PKCE with `/token` HTTP 200, preserved the exact Auth UUID, bootstrapped its audited owner membership, and rendered the Starfiniti / Main Store dashboard through RLS. The dashboard container's split-DNS route and PostgREST `loyalty` schema allowlist were verified live.
- Final authenticated Chrome verification on 2026-08-13 rendered the deployed `v0.1.10` Programme overview, Earning rules, Rewards, and VIP tiers from live Starfiniti / Main Store state. Exact active navigation, `€200 → Bloom → 1,200 points` simulation, first-reward setup, threshold-overlap blocking, mobile drawer behavior, and 390-pixel no-overflow all passed with zero browser warnings/errors; both exact-SHA application containers and the public dependency health route were healthy.
- PR exact-head run `31512548299` passed the baseline and Docker/Supabase database jobs.
- Public `main` run `31513294330` passed both jobs after merge, including migration replay, seed, pgTAP, and cleanup.
- PR exact-head run `31527785181` passed both jobs, including replay/reset/seed, all 87 pgTAP assertions, the dynamic ingestion build, and cleanup.
- PR exact-head run `31566530867` passed both jobs, including four migration replays, reset/seed, all 178 pgTAP assertions, a competing-reservation test, 20 deterministic operation sequences, and cleanup.
- PR exact-head run `31569179555` passed both jobs, including five migration replays, reset/seed, all 260 pgTAP assertions, ledger overspend, concurrent evaluation idempotency, the property probe, and cleanup.
- PR exact-head run `31575751260` passed the full baseline and Docker/Supabase jobs with six migrations, 322 pgTAP assertions, concurrency/property probes, and cleanup.
- PR exact-head run `31577312529` passed all six jobs: baseline, Docker/Supabase, and minimum/current WooCommerce runtimes in HPOS and legacy modes.
- PR exact-head run `31580836101` passed all six jobs with seven migration replays, 374 pgTAP assertions, concurrency/property probes, and the complete minimum/current HPOS/legacy WooCommerce matrix.
- PR exact-head run `31581760825` passed all six jobs for the customer-ledger slice, including the clean Next.js build, seven-migration database verification, and complete WooCommerce matrix.
- PR exact-head run `31584171545` passed all six jobs for connector operations, including 412 pgTAP assertions and the complete WooCommerce matrix.
- PR exact-head run `31584351529` passed all six jobs for customer adjustments, including nine migration replays, 448 pgTAP assertions, and the complete WooCommerce matrix.
- PR exact-head run `31585681985` passed all six jobs for signed source reconciliation, including ten migration replays, 485 pgTAP assertions, and the complete WooCommerce matrix.
- PR exact-head run `31588394642` passed all six jobs for live Overview reporting, including eleven migration replays, 518 pgTAP assertions, concurrency/property probes, and the complete WooCommerce matrix.
- PR exact-head run `31589866616` passed all six jobs for exact customer reads, including twelve migration replays, 551 pgTAP assertions, concurrency/property probes, and the complete WooCommerce matrix; one matrix cell passed on retry after an external Composer TLS certificate mismatch.
- PR exact-head run `31591151097` passed all six jobs for initial-programme onboarding, including thirteen migration replays, 586 pgTAP assertions, concurrency/property probes, and the complete WooCommerce matrix.
- PR exact-head run `31592427051` passed all six jobs for customer tier visibility, including fourteen migration replays, 613 pgTAP assertions, concurrency/property probes, and the complete WooCommerce matrix.
- `npm run accessibility:validate`, dashboard lint/typecheck, all 27 dashboard tests, rendered DOM inspection, and initial desktop/mobile authentication captures exercised the accessibility slice locally; the mobile capture exposed a card overflow, and the corrected shrinkable sizing is now guarded statically.
- PR exact-head run `31596460783` passed all six jobs for the accessibility slice, including fourteen migration replays, 613 pgTAP assertions, concurrency/property probes, and the complete WooCommerce matrix.
- PR exact-head run `31597255280` passed all six jobs for sanitized support diagnostics, including fourteen migration replays, 613 pgTAP assertions, concurrency/property probes, and the complete WooCommerce matrix.
- PR exact-head run `31598618092` passed all six jobs for WooCommerce localization, including fourteen migration replays, 613 pgTAP assertions, and real Slovenian catalog loading in every minimum/current HPOS/legacy runtime.
- PR exact-head run `31600742177` passed all six jobs for controlled experience themes, including fifteen migration replays, 654 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31601351946` passed all six jobs for filtered customer activity, including 109 unit tests, fifteen migration replays, 654 pgTAP assertions, and the complete localized WooCommerce matrix.
- PR exact-head run `31603764054` passed all six jobs for controlled bulk customer adjustments, including sixteen migration replays, 693 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31604654919` passed all six jobs for enforced storefront budgets, including zero-request bounded account/cart rendering in all four localized WooCommerce runtime variants plus the unchanged sixteen-migration, 693-assertion database gate.
- PR exact-head run `31606226276` passed all six jobs for hosted customer translations, including 114 unit tests, seventeen migration replays, 726 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31608392260` passed all six jobs for hosted guest loyalty delivery, including 119 unit tests, eighteen migration replays, 752 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31618909782` passed all six jobs for signed authenticated customer delivery, including 124 unit tests, twenty migration replays, 818 pgTAP assertions, concurrency/property probes, and locally signed PII-free claim links in the complete localized WooCommerce matrix.
- PR exact-head run `31622879767` passed all six jobs for controlled customer reward redemption, including 126 unit tests, twenty-one migration replays, all 863 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31625573608` passed all six jobs for WooCommerce customer erasure, including 128 unit tests, twenty-two migration replays, all 910 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31627622779` passed all six jobs for hosted customer localization, including 132 unit tests, twenty-two migration replays, all 910 pgTAP assertions, concurrency/property probes, and active-locale claim links in the complete localized WooCommerce matrix.
- PR exact-head run `31629852692` passed all six jobs for hosted customer export, including 141 unit tests, twenty-three migration replays, all 953 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31633310240` passed all six jobs for guided WooCommerce provisioning, including 146 unit tests, twenty-four migration replays, all 997 pgTAP assertions, concurrency/property probes, and real setup-package import in the complete localized WooCommerce matrix.
- PR exact-head run `31634024586` passed all seven jobs for deployment artifacts, including real digest-pinned dashboard/worker Docker builds, the baseline, twenty-four migration replays, all 997 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31635189128` passed all seven jobs for dependency-aware readiness, including 147 unit tests, real dashboard/worker Docker builds, twenty-four migration replays, all 998 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31636596218` passed all seven jobs for audited initial-tenant bootstrap, including 147 unit tests, real dashboard/worker Docker builds, twenty-five migration replays, all 1,028 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31638672681` passed all seven jobs for the localized merchant Overview shell, including 151 unit tests, both production images, twenty-five migration replays, all 1,028 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31640311302` passed all seven jobs for the localized programme workflow and Europe/Ljubljana scheduling boundary, including 154 unit tests, both production images, twenty-five migration replays, all 1,028 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31640919355` passed all seven jobs for localized connector provisioning and operations, including 154 unit tests, both production images, twenty-five migration replays, all 1,028 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31642070490` passed all seven jobs for localized customer administration and explicit Europe/Ljubljana adjustment expiries, including 154 unit tests, both production images, twenty-five migration replays, all 1,028 pgTAP assertions, concurrency/property probes, and the complete localized WooCommerce matrix.
- PR exact-head run `31642918324` passed all seven jobs for localized experience administration, including the bounded customer-locale preview, both production images, the unchanged database gate, and the complete localized WooCommerce matrix.
- PR exact-head run `31643300533` passed all seven jobs for the localized document-language and keyboard-bypass shell, including the full repository check, production images, database gate, and four WooCommerce runtimes.

## Next recommended task

Review ready PR #57, then activate only approved external closeouts after a separate merge/release decision: the real WooCommerce pilot, monitoring/recovery/security exercises, module canaries, the thirty-day GA window, and the real monthly/quarterly M16 cadence. Do not substitute repository fixtures or green merge checks for production or elapsed evidence.

## Blockers

Repository and Proxmox access, DNS/TLS, off-host PostgreSQL backup, production credentials, Authentik SSO, and the initial owner tenant are available. M01 still requires an explicitly approved real WooCommerce store, and full application/Auth/signing clean-room recovery remains unproved. Exact Stripe/Klaviyo/enterprise-IdP credentials remain deferred to their module gates. M15 cannot close until all prior module closeouts, live capacity/fault/recovery/monitoring exercises, scanner/triage/release/production/penetration evidence, a 30-day immutable-release canary, exact reconciliation, claims review, operational ownership, and explicit GA approvals pass. M16 initial close additionally requires named schedules/owners, two elapsed consecutive monthly reviews, one reconciled quarterly exercise bundle, current provider/dependency review, independent review, and five approvals.
