# Identity Model

Channel identities use `(connection_id, external_customer_id)` as a primary external key. Email and phone are attributes, not merge authority. Cross-brand or cross-workspace wallet sharing requires an explicit programme-group policy and auditable linking decision.
