# M07 Evidence — Campaigns

M07 is in progress. ADR-0021 and M07-S01 define the first authority boundary: strict allowlisted predicates over canonical loyalty facts, immutable audience versions and database-timed snapshots, exact TypeScript/PostgreSQL parity, private PII-free membership evidence, tenant-derived commands, and entitlement-safe rollback.

Exact-head Ubuntu run [`31773939480`](https://github.com/Starfiniti/starfiniti-loyalty/actions/runs/31773939480) passed at `8192dd7`: the complete repository gate, both production images, a clean 42-migration replay, all 36 pgTAP files with 1,770 assertions including 70 focused audience assertions, all four concurrency probes, 126 dashboard tests, 21 worker tests, 143 contract tests, 53 domain tests, 15 accepted ADRs, and all four minimum/current HPOS/legacy WooCommerce runtimes. The audience race created one immutable snapshot/member/audit and returned one `created` plus one `duplicate` result across two sessions.

M07-S01 is complete. The first run exposed an explicit UUID fixture cast and global reviewed-function allowlist omissions; the second exposed a PL/pgSQL record/alias collision. Adversarial review then added independent input-size, public-identity/hash, candidate-loop, and rollback-safe retry protections. Each failure was deterministic, forward-fixed, and retained in CI history; no blocker or should-fix finding remains. No production deployment or entitlement enablement is claimed.

M07-S02 is complete at the repository gate. Exact-head Ubuntu run [`31777177961`](https://github.com/Starfiniti/starfiniti-loyalty/actions/runs/31777177961) passed at `e92f52d`: the complete repository gate, both production images, a clean 43-migration replay, all 37 pgTAP files with 1,851 assertions including 81 focused campaign assertions, all four concurrency probes, 126 dashboard tests, 21 worker tests, 157 contract tests, 53 domain tests, 16 accepted ADRs, and all four minimum/current HPOS/legacy WooCommerce runtimes.

The accepted boundary includes `CampaignDefinitionV1`, 14 focused contract tests, ADR-0022, all seven strict behaviors, explicit-offset/IANA/local schedule parity, DST gap/overlap handling, hard effect/points/liability ceilings, exact decimal RPC integers including empty previews, same-tenant immutable inclusion/exclusion binding, private approval-time treatment/control assignment with exact reviewed aggregate counts, lifecycle roles, original-outcome idempotency, and rollout-safe pause/cancel. The deterministic CI loop retained and forward-fixed migration expression, fixture identity, lifecycle fixture, and immutable-retry fixture failures. Adversarial review then replaced probabilistic control thresholds with exact salted-hash ranks, aligned public/database reason validation, and made preview arithmetic and large integers exact. No blocker or should-fix finding remains. S02 deliberately activates no schedule and issues no value; the completed M07-S03 boundary is recorded below.

M07-S03 is complete at the repository gate. Exact-head Ubuntu run [`32662380030`](https://github.com/Starfiniti/starfiniti-loyalty/actions/runs/32662380030) passed at `c82f3ed`: baseline, both production images, a clean 44-migration replay, all 38 pgTAP files with 1,914 assertions including 62 focused capacity/execution assertions, all four concurrency probes, 126 dashboard tests, 22 worker tests, 160 contract tests, 57 domain tests, 17 accepted ADRs, and all four minimum/current HPOS/legacy WooCommerce runtimes.

ADR-0023 binds one stable operation/member/campaign lock order to private effect/points/liability counters, immutable treatment/control context, independent PostgreSQL decision reconstruction, the existing ProgrammeDefinitionV2 award boundary, separate campaign-attributed ledger transactions, and original-outcome replay. Fixed bonuses stack, only the highest-priority eligible programme-or-campaign multiplier wins, and non-purchase campaigns receive a reservation primitive that cannot overrun global, member, points, or liability ceilings. The two-session probe proved one last-budget reservation wins, the competitor observes exhaustion, exact retry is idempotent, and counters reconcile.

The deterministic CI loop found and forward-fixed a duplicate programme/campaign ledger source identity, then corrected two fixture assertions without weakening the 62-test plan. Campaign awards now use bounded campaign-specific operation references while retaining the same canonical commerce event. Manual adversarial review found no remaining tenant, privilege, idempotency, capacity, ledger, or rollback blocker. S03 itself activated no scheduler or managed rollout; the completed S04 boundary is recorded below.

M07-S04 is complete at the repository gate. Exact-head Ubuntu run [`32667482144`](https://github.com/Starfiniti/starfiniti-loyalty/actions/runs/32667482144) passed at `0b60d1b`: the complete baseline, both production images, a clean 45-migration replay, all 38 pgTAP files with 1,967 assertions including 115 focused campaign capacity/trigger assertions, all five two-session concurrency probes, 126 dashboard tests, 25 worker tests, 167 contract tests, 57 domain tests, 17 validator-tracked accepted architecture decisions, and all four minimum/current HPOS/legacy WooCommerce runtimes.

ADR-0024 binds every stable campaign, purchase context, and non-purchase source history to one exact programme. Database-owned milestone, win-back, tier, referral, and limited-assignment facts append minimized private jobs; the worker can only schedule, claim, execute, or finish bounded leases. Expired-lease recovery is batch-limited, attempt evidence is written only for rows actually transitioned, and the tenth claim stops in inspectable manual review. Point grants, immediate release/expiry evidence, capacity, and execution commit atomically. Native grants use the existing WooCommerce state machine with campaign-funded control-to-reserved ledger evidence and never debit member available points. Refunds cancel unleased work or append one linked reversal/cancellation outcome; ambiguous native results retain value and capacity.

The deterministic loop forward-fixed function ownership/revocation ordering, queue constraints, PostgreSQL conditional syntax, a control-account assertion that incorrectly trusted a projection, cross-programme milestone/win-back history, exact purchase-context programme binding, entitlement gating for new issue work, bounded concurrent lease recovery, same-group fixture publication/selectors, and a test that correctly failed when `loyalty_worker` attempted direct private-table inspection. No test plan was weakened. Final adversarial review found no remaining security, tenant, ledger, idempotency, retry, refund, or rollback blocker. No production schedule, entitlement canary, or campaign value is claimed; the completed S05 boundary is recorded below.

M07-S05 is complete at the repository and browser gates. Exact-head Ubuntu run [`32671197966`](https://github.com/Starfiniti/starfiniti-loyalty/actions/runs/32671197966) passed at `fcb1f3c`: the complete baseline, both production images, a clean 46-migration replay, all 38 pgTAP files with 1,975 assertions including 123 focused campaign assertions, all five concurrency probes, 139 dashboard tests, 25 worker tests, 170 contract tests, 57 domain tests, 19 validator-tracked ADRs, and all four minimum/current HPOS/legacy WooCommerce runtimes.

ADR-0025 and the strict `CampaignResultV1` boundary add the complete merchant command center without exposing private campaign evidence. Owners/admins can create and edit immutable audiences/campaigns, publish and freeze audiences, build all seven behaviors, preview and explicitly confirm liability, approve schedules, and cancel; operators can snapshot, preview, and pause; analysts/auditors inspect. The aggregate projection derives live membership and exact programme scope, returns exact decimal text, and excludes member assignments, source references, retry errors, salts, actors, customer identities, and coupon material. Metric definitions distinguish operational facts and influenced outcomes from unmeasured experimental incrementality.

The first exact-head run retained a deterministic failure: the new projection incorrectly schema-qualified PostgreSQL's special `coalesce` expression and was absent from two deliberate security-definer allowlists. Commit `fcb1f3c` fixed both without reducing the 123-test plan. Production-build browser evidence and the exact interaction matrix are recorded in `merchant-experience-2026-08-24.md`. M07-S06 reviewed stacked merge, disabled deployment, Starfiniti canary, full reconciliation, and module scoring remain open; no production campaign schedule or value is claimed.

Record deployment, Starfiniti canary, exact balance/capacity/reward reconciliation, rollback, smoke, and score evidence here during S06.
