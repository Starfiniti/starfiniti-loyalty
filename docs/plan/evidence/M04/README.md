# M04 Evidence — Rewards and Fulfilment

Status: in progress. M04-S01 through M04-S03 are complete. Native and manual reward contracts, database capacity and fulfilment boundaries, capability-negotiated WooCommerce execution, merchant/customer workflows, all 1,302 pgTAP assertions, both concurrency probes, both production images, and all four WooCommerce runtime environments passed PR #29 exact-head run `31747964152`. The real production editor and fulfilment components then passed native-Chrome desktop, mobile, 320 px, keyboard, reduced-motion, invalid-submit, recovery-state, accessibility, and no-overflow review; see `native-rewards-2026-08-13.md` and `rewards-browser-qa-2026-08-27.md`.

M04-S04 now has a fail-closed machine gate in [`canary.yaml`](canary.yaml), validated during `npm run check`. It requires 48 unique minimized checks, nine named production artifacts, exact score arithmetic, at least 90/100 overall, at least 80% in every category, four explicit approvals, completed prerequisite slices, no failed or pending check, and matching completed task state before it can claim completion. Verified artifacts are unique, safe bounded JSON files under the M04 evidence root with exact SHA-256 digests, exact check coverage, the exact non-production candidate commit, and the semantic contracts in [`ARTIFACT_CONTRACT.md`](ARTIFACT_CONTRACT.md). The validator reconciles approved policies and numeric ceilings, released images/plugin/contracts, reward behavior and recovery evidence, exact scenario counts, zero-difference value/queue/privacy outcomes, canonical release-to-canary chronology, rollback after canary end, and an observation of at least 24 hours. Adversarial fixtures reject hollow, relabelled, mismatched, nonzero, stale, or short evidence. The provisional score is 90/100, but operability is 3/10 and below its mandatory 8/10 floor.

Still required under M04-S04: approved exact release and pilot store, fresh recovery point, disabled deployment, Starfiniti-only canary, native/manual value reconciliation, rollback/observation evidence, and final scoring with every category above its floor.
