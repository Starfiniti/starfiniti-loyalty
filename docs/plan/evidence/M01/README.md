# M01 Evidence — Production WooCommerce Pilot

Status: in progress. No real-store value has been accepted. `pilot.yaml` is the validated gate state; `production-baseline-2026-08-28.md` remains the current complete read-only production baseline, while the 2026-08-13 baseline is historical evidence. `backup-transfer-amplification-2026-08-14.md` records containment and replacement of the internal full-tree backup transfer loop. `supabase-compatibility-2026-08-28.json` is immutable historical compatibility evidence, and `supabase-compatibility-2026-09-01.json` hash-binds it while recording ADR-0123's approved Studio-only recreation. Studio now uses the exact three-schema runtime contract on the same reviewed healthy image; the other ten services, a post-change PostgreSQL archive, public health/login, anonymous Auth/Data API denial, and zero protected commerce/value aggregates all passed. Exact future Supabase upgrade rehearsal and full clean-room application/Auth/configuration/database restore remain pending. Whole-VM archive creation covers both production VMs, but no full application/Auth/signing restoration is claimed. Execute `docs/operations/WOOCOMMERCE_PILOT.md` only after explicit store approval.
