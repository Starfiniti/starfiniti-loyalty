global:
  resolve_timeout: 5m

route:
  receiver: unconfigured
  group_by: [alertname, environment, service]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 15m

receivers:
  - name: unconfigured
